# Copy this file to .env and fill in your own values. Never commit .env itself.

GCP_PROJECT_ID=your-gcp-project-id
GCS_BUCKET_NAME=your-crypto-pipeline-bucket
BQ_DATASET_RAW=raw_crypto
BQ_TABLE_RAW=crypto_prices
BQ_LOCATION=US

# Number of coins to pull per run (CoinGecko markets endpoint, max 250 per page)
COINGECKO_TOP_N=100
